// Set this to your Render backend's public URL when this frontend is
// deployed to Cloudflare Pages (or anywhere other than the Render
// service itself). Leave it as '' to keep using same-origin requests,
// which is correct when Node is still serving these files directly.
//
// Example: window.VIEWER_COUNTER_API_BASE = 'https://viewer-counter.onrender.com';
window.VIEWER_COUNTER_API_BASE = '';
